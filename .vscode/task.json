{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "cargo build",
            "command": "cargo",
            "type": "shell",
            "args": [
                "build",
                "--release",
                "--target x86_64-unknown-linux-musl"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
